#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../icon-font-generator/bin/icon-font-generator" "$@"
  ret=$?
else 
  node  "$basedir/../icon-font-generator/bin/icon-font-generator" "$@"
  ret=$?
fi
exit $ret
